{#
/**
 * @file
 * Theme override to display a region.
 *
 * @see template_preprocess_region()
 */
#}
{%
  set classes = [
    'region',
    region|clean_class|replace({'-': ' '}),
  ]
%}
{% if content %}
  <{{ tag }}{{ attributes.addClass(classes) }}>
    {{ content }}
  </{{ tag }}>
{% endif %}
